<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:background="@drawable/bg_item_nfc_card_shape"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/iv_nfc_card_bg"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:scaleType="fitXY"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_nfc_card_home" />

    <TextView
        android:id="@+id/tv_nfc_card_nickname"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:textColor="@color/white"
        android:textSize="@dimen/text_18sp"
        app:layout_constraintStart_toStartOf="@id/iv_nfc_card_bg"
        app:layout_constraintTop_toTopOf="@id/iv_nfc_card_bg"
        tools:text="家" />

    <TextView
        android:id="@+id/tv_nfc_card_default_flag"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="16dp"
        android:textColor="@color/white"
        android:textSize="@dimen/text_14sp"
        app:layout_constraintBottom_toBottomOf="@id/tv_nfc_card_nickname"
        app:layout_constraintEnd_toEndOf="@id/iv_nfc_card_bg"
        app:layout_constraintTop_toTopOf="@id/tv_nfc_card_nickname"
        android:text="默认卡片" />

    <ImageView
        android:id="@+id/iv_nfc_card_edit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_device_info"
        android:padding="8dp"
        android:visibility="gone"
        android:layout_marginEnd="12dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>
</androidx.constraintlayout.widget.ConstraintLayout>